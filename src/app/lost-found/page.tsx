import { LostFound } from '@/views/LostFound';

export default function LostFoundPage() {
  return <LostFound />;
}

export const metadata = {
  title: 'Lost & Found Animals | Pet Connect',
  description:
    'Report a lost or found animal, or browse existing reports to help reunite pets with their owners.',
};
